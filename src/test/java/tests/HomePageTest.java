package tests;

import utility.AppConstants;
import utility.BaseTest;

public class HomePageTest extends BaseTest implements AppConstants.APP_PARAMETERS {

    /*@Test(priority = 1, testName = "Get Login Page Title")
    public void loginPageTitleTest() {
        String actualTitle = loginPage.getLoginPageTitle();
        Assert.assertEquals(actualTitle, GEAR_BOX_TITLE);
    }

    @Test(priority = 2, testName = "Get Login Page URL")
    public void loginPageUrlTest() {
        String actualUrl = loginPage.getLoginPageUrl();
        Assert.assertEquals(actualUrl, EXPECTED_URL);
    }

    @Test(priority = 3, testName = "Successful Login Gearbox")
    public void successfulLoginToGearbox() {
        loginPage.enterEmailId(LOGIN_EMAIL_ID);
        loginPage.enterPassword(PASSWORD);
        loginPage.clickOnLoginBtn();
    }*/
}
